# Source (only dist/ needed)
src/
tsconfig.json

# Secrets & user config
.env
mcp-config.json

# Marker Python environment (too large, user installs separately)
marker-worker/.venv/
marker-worker/.cache/
marker-worker/__pycache__/
marker-worker/local.env
marker-worker/smoke.pdf
marker-worker/uv.lock
marker-worker/.tools/

# Runtime artifacts
downloads/
scihub-mirrors.txt
node-out.txt
temp_*.html
test-results.txt

# Dev & IDE
.git/
.gitignore
.claude/
.vscode/
.idea/
.tools/

# Skill (Claude Code specific, not needed in bundle)
skills/

# npm publish metadata (not needed in bundle)
package-lock.json

# Example config (manifest user_config replaces this)
mcp-config.example.json
